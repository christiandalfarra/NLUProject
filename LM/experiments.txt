Part A

all the training ar done with clip = 5 , n_epochs = 100 , patience = 3

first experimment was simply increment the lr from 0.0001 to 0.01
second experiment a grid search for the other parameters of the net emb size = [250,300,350], hid size = [250,300,350], the training was done with two lr = [0.01, 0.05]
third experiment try the best net founded by grid search (emb 350, hid 350) with other values of lr = [0.1, 0.5, 1, 2, 5] improvement only for 0.1, 0.5

tutti quelli su questa tabella hanno il modulo RNN
(e = emb_size, h = hid_size, l = lr)
|    Model spec     |  PPL  |
|-------------------|-------|
| e350 h350 lr0.5   | 167.70|

fourth experiment change the RNN module with LSTM one, test with lr 0.5
|   LSTM l0.5       | 146.57|

fifth experiment add dropout layer after the embedding one embdo=0.5

|LSTM l0.5 doemb0.5 | 132.09| best LSTM embdo

sixth experiment add dropout layer before the last linear layer
|LSTM l0.5 doemb0.5 | 131.21|
        dolast0.2

|LSTM l0.5 doemb0.2 | 000.00|
        dolast0.2
seventh experiment change the optimizer with the AdamW 
take the model with LSTM dropout before the last and after the emb and change the optimizer
try with different learning rate

|    LSTM l0.0001   | 121.17|

part B

Starting from the `LM_RNN` in which you replaced the RNN with a LSTM model, apply the following regularisation techniques:
- Weight Tying 
- Variational Dropout (no DropConnect)
- Non-monotonically Triggered AvSGD 
